import { defineMessages } from 'react-intl';

export default defineMessages({
  greeting: {
    id: 'app.home.greeting',
      description: 'this is a welcomme message',
      defaultMessage: 'This is Home',
  }
});
